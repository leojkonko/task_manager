<?php
$title = 'Nova Tarefa';
$this->headTitle($title);
?>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h3 class="mb-0"><?= $this->escapeHtml($title) ?></h3>
                </div>
                <div class="card-body">
                    <form method="post" id="taskForm">
                        <div class="mb-3">
                            <label for="title" class="form-label">Título da Tarefa *</label>
                            <input type="text" class="form-control" id="title" name="title" required 
                                   value="<?= $this->escapeHtmlAttr($_POST['title'] ?? '') ?>">
                        </div>

                        <div class="mb-3">
                            <label for="description" class="form-label">Descrição</label>
                            <textarea class="form-control" id="description" name="description" rows="4" 
                                      placeholder="Descreva os detalhes da tarefa..."><?= $this->escapeHtml($_POST['description'] ?? '') ?></textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="status" class="form-label">Status</label>
                                    <select class="form-select" id="status" name="status">
                                        <?php foreach ($availableStatuses as $key => $label): ?>
                                            <option value="<?= $this->escapeHtmlAttr($key) ?>" 
                                                    <?= ($_POST['status'] ?? 'pending') === $key ? 'selected' : '' ?>>
                                                <?= $this->escapeHtml($label) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label for="priority" class="form-label">Prioridade</label>
                                    <select class="form-select" id="priority" name="priority">
                                        <?php foreach ($availablePriorities as $key => $label): ?>
                                            <option value="<?= $this->escapeHtmlAttr($key) ?>" 
                                                    <?= ($_POST['priority'] ?? 'medium') === $key ? 'selected' : '' ?>>
                                                <?= $this->escapeHtml($label) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="due_date" class="form-label">Data de Vencimento</label>
                            <input type="datetime-local" class="form-control" id="due_date" name="due_date" 
                                   value="<?= $this->escapeHtmlAttr($_POST['due_date'] ?? '') ?>">
                            <div class="form-text">Deixe em branco se não houver prazo específico</div>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="<?= $this->url('task-manager') ?>" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Voltar
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Salvar Tarefa
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
document.getElementById('taskForm').addEventListener('submit', function(e) {
    const title = document.getElementById('title').value.trim();
    if (!title) {
        e.preventDefault();
        alert('O título da tarefa é obrigatório');
        document.getElementById('title').focus();
        return false;
    }
});
</script>